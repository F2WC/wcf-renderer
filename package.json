{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "playground/*"
  ],
  "scripts": {
    "dev": "nx run-many -t dev -t server --projects=sdk,shell-package,mfe-vue-one,mfe-react-one,shell-app,server --parallel=6",
    "build": "lerna run build",
    "test": "lerna run test",
    "test:watch": "lerna run test:watch",
    "release": "lerna version",
    "lint:script": "eslint . && prettier --check .",
    "lint:script:fix": "eslint --fix . && prettier --write ."
  },
  "dependencies": {},
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "eslint": "^9.31.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.3",
    "lerna": "^9.0.1",
    "nx": "^22.0.4",
    "prettier": "^3.6.2",
    "typescript-eslint": "^8.37.0",
    "vite": "^7.3.1"
  }
}
